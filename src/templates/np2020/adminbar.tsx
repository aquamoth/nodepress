// eslint-disable-next-line @typescript-eslint/no-unused-vars
import * as React from "react";
import { ViewHelper } from "@core/types/viewengine";

export default (model: {}, viewHelper: ViewHelper) => ({
    component: (<>
        <h1>Here the admin bar goes</h1>
    </>)
});
